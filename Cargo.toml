[package]
name = "draft"
version = "0.1.0"
edition = "2024"

[dependencies]
frame-graph = { git = "https://github.com/zuiyu1998/frame-graph.git", features = [
    "serde",
] }
fyrox-core = { git = "https://github.com/zuiyu1998/Fyrox.git" }
fyrox-resource = { git = "https://github.com/zuiyu1998/Fyrox.git" }

naga = { version = "25.0", features = ["serialize", "deserialize"] }
serde = { version = "1.0", features = ["derive"] }
ron = "0.8.0"
thiserror = { version = "1.0"}

[dev-dependencies]
winit = "0.30"
tracing-subscriber = { version = "0.3" }
