[package]
name = "draft_window"
version = "0.1.0"
edition = "2024"

[dependencies]
draft_core = { path = "../draft_core" }

downcast-rs = "2.0"
raw-window-handle = { version = "0.6", features = [
    "alloc",
], default-features = false }
