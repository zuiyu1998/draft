[package]
name = "draft_render"
version = "0.1.0"
edition = "2024"

[dependencies]
draft_window = { path = "../draft_window" }

fyrox-resource = { workspace = true }
fyrox-core = { workspace = true }
frame-graph = { workspace = true }

wgpu = { version = "27" }

futures-lite = { version = "2.6" }
