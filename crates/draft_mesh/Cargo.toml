[package]
name = "draft_mesh"
version = "0.1.0"
edition = "2024"

[dependencies]
draft_graphics = { path = "../draft_graphics" }

fyrox-core = { version = "1.0" }
fyrox-resource = { version = "1.0" }
